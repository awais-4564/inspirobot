import 'package:flutter/material.dart';

import '../model/tint_model.dart';

class Tints {
  List<TintModel> list() {
    return [
      TintModel(color: Colors.orange),
      TintModel(color: Colors.green),
      TintModel(color: Colors.yellow),
      TintModel(color: Colors.blue),
      TintModel(color: Colors.pink),
      TintModel(color: Colors.red),
      TintModel(color: Colors.purple),
      TintModel(color: Colors.blue),
    ];
  }
}
